import React from "react";

//* this page use to add custom not found page
//* here we can add not found custom ui

const NotFound = () => {
  return <div>oppps...</div>;
};

export default NotFound;
